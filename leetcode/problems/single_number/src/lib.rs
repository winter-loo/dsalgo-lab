pub struct Solution;

impl Solution {
    // LeetCode 136: Single Number
    // Function to find the single element in an array where every element appears twice except for one
    pub fn single_number(nums: Vec<i32>) -> i32 {
        todo!()
    }
}
